echo "# -myrepo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:vamshi-bolla/-myrepo.git
git push -u origin main

git status
git add .
git rm -rf "filename"---> To delete file
git log ---> To see all the history.
git log --pretty=oneline---> To see comments in one line instead of multiple lines.
git show <<version number to see the details>>
git tag
git tag V1 -m "Your Comments"---> To Tag source code
git push origin V1--->Push tag to git, versioning is case sensitive ?
git clone <<URL>>---> To download code from git to local 
git remote -v  --->To see the fetch/push origins
git checkout/switch -b/-c <<Branch Name>>---> To create a branch
git branch--->To list down all branches (* indicates current branch)
git branch --all --->To list down all branches including remtoe branches (* indicates current branch)
git checkout/switch <<Branch Name>>---> To switch between branches
gti checkout/switch - --->To go back to previous branch
gti branch -d <<Branch Name>> --->To delete branch